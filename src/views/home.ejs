<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <link rel = "stylesheet" href="/css/styles.css">
</head>


<body>
    <div class="topnav">
        <a class="active href="/">Home</a>
        <a href="/create"  >Creat User</a>
      </div>
      <div class= "table-container" >
        <!-- <%= JSON.stringify(ListUsers) %> -->
        <div class = "tilte"> List Users</div>
          
      <table>
        <tr>
          <th>ID</th>
          <th>Email</th>
          <th>Name</th>
          <th>City</th>
          <th>Action</th>
        </tr>
        
          <!-- <% for (let i = 0; i < ListUsers.length; i++) { %>
            <tr>
              <td> <%= ListUsers[i].id %> </td>
              <td><%= ListUsers[i].email %></td>
              <td><%= ListUsers[i].name %></td>
              <td><%= ListUsers[i].city %></td>
              <td><button class="editbtn">edit</button>
                <button class="delbtn">Delete</button>
              </td>
            </tr>
            <% } %> -->

            <% ListUsers.forEach(function(user, index) { %>
              <tr>
                <td> <%= user.id %></td>
                <td><%= user.email %></td>
                <td><%= user.name %></td>
                <td><%= user.city %></td>
                <td><a href = "/update/<%= user.id %>"><button>Edit</button></a> 

                  <form action="delete-user/<%= user.id %>" method="post"><button>Delete
                  </button>
                  </form>
                                    
                </td>
              </tr>
           <% }); %> 
          </ul>

      </table>
    </div>
 
</body>
</html>